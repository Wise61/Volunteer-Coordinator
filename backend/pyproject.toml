[project]
name = "backend"
version = "0.1.0"
description = "Backend for the Volunteer Coordinator app."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.1",
]
